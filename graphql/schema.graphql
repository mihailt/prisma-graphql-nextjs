type Query {
  posts(limit: Int!, cursor: String, userId: String): [Post]
  user(id: String!): User
}

type Post {
  id: ID!
  title: String!
  description: String!
  url: String!
  imageUrl: String!
  user: User!
}

type User {
  id: ID!
  email: String!
  image: String
}